= turbo_frame_tag @room do
  .labels
    %span= @room.name
  %span Are you sure you want to delete this room?
  .actions
    &= button_to "Yes", { action: "destroy", id: @room.id },
                                method: :delete, target: 'rooms', 'data-turbo-frame' => "room_#{@room.id}"
    = link_to 'No', target: '_top'